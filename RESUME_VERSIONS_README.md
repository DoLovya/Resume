# 简历多版本管理规范

为了满足不同求职场景下的简历投递需求，本项目建立了结构化的多版本简历管理目录。以下是目录架构、命名规范及使用指南。

## 1. 目录架构

在项目根目录下，保留 `resume.json` 作为 **基础模板（Master Template）**，所有衍生版本简历平铺存放在 `resume_versions/` 目录中。

```text
resume_versions/
├── 张欢-C++客户端开发.json
└── 张欢-工业视觉客户端开发.json
```

- **根目录 `resume.json`**：始终作为所有简历派生的标准基础模板，包含最全的通用信息。
- **`resume_versions/` 目录**：存放针对特定岗位优化过内容或样式的衍生 `.json` 文件。

## 2. 简历命名规范

为清晰区分各版本，所有存放在 `resume_versions/` 目录中的简历文件，遵循以下极简的命名规范：

**格式**：`[姓名]-[投递岗位].json`

**示例**：
- `张欢-C++客户端开发.json`（针对纯客户端界面、交互及基础设施建设的开发岗位）
- `张欢-工业视觉客户端开发.json`（针对突出算法链路接入、图像处理性能优化的工业质检或视觉软件平台岗位）

## 3. 使用指南

- **生成特定简历**：若需使用某岗位的简历，将 `resume_versions/` 中对应的 JSON 文件复制到项目根目录并重命名为 `resume.json`（注意备份原文件），即可直接通过现有生成工具渲染并导出 PDF。
- **扩展版本**：若有新的岗位投递需求，直接在 `resume_versions/` 目录下按 `姓名-岗位.json` 格式新建文件即可。
